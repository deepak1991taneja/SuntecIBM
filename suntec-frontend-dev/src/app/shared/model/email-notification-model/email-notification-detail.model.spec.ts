import { EmailNotificationDetail } from './email-notification-detail.model';

describe('EmailNotificationDetail', () => {
  it('should create an instance', () => {
    expect(new EmailNotificationDetail()).toBeTruthy();
  });
});
